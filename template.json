{
  "name": "NodePulse",
  "description": "AI-powered DePIN operations platform that predicts failures before they happen, optimizes earnings with intelligent recommendations, and benchmarks performance against the network. The only platform that doesn't just monitor—it makes you money.",
  "version": "2.0.0",
  "category": "monitoring",
  "logo_url": "https://raw.githubusercontent.com/TheSoftNode/NodePulse/main/public/logo.png",
  "documentation_url": "https://github.com/TheSoftNode/NodePulse",
  "repository_url": "https://github.com/TheSoftNode/NodePulse",
  "env_variables": {
    "MONGODB_URI": {
      "required": true,
      "description": "MongoDB connection string (MongoDB Atlas or local)",
      "default": "mongodb+srv://nodepulse:nodepulse@nodepulse.a0ewlvl.mongodb.net/nodepulse?appName=nodepulse"
    },
    "CRON_SECRET": {
      "required": true,
      "description": "Secret key for cron job authentication - used to secure automated health check endpoint",
      "generate": true
    },
    "DISCORD_WEBHOOK_URL": {
      "required": false,
      "description": "Discord webhook URL for sending alerts (optional)"
    },
    "TELEGRAM_BOT_TOKEN": {
      "required": false,
      "description": "Telegram bot token for sending alerts (optional)"
    },
    "TELEGRAM_CHAT_ID": {
      "required": false,
      "description": "Telegram chat ID for receiving alerts (optional)"
    },
    "CUSTOM_WEBHOOK_URL": {
      "required": false,
      "description": "Custom webhook URL for sending alerts (optional)"
    },
    "HELIUM_API_BASE": {
      "required": false,
      "description": "Helium API base URL",
      "default": "https://api.helium.io/v1"
    },
    "RENDER_API_BASE": {
      "required": false,
      "description": "Render API base URL",
      "default": "https://api.render.com/v1"
    },
    "NODE_ENV": {
      "required": true,
      "description": "Node environment",
      "default": "production"
    },
    "PORT": {
      "required": true,
      "description": "Application port",
      "default": "3000"
    }
  },
  "ports": [
    {
      "container": 3000,
      "public": 3000,
      "description": "NodePulse web dashboard"
    }
  ],
  "volumes": [
    {
      "container": "/app/data",
      "description": "Application data directory"
    }
  ],
  "healthcheck": {
    "endpoint": "/api/health",
    "interval": 30,
    "timeout": 10,
    "retries": 3
  },
  "features": [
    "🤖 AI-powered predictive failure detection (87% accuracy, 6+ hours advance warning)",
    "💰 Earnings optimizer with actionable recommendations (+23% avg increase)",
    "📊 Network health score - see how you rank vs thousands of operators",
    "🎯 ROI comparison across different DePIN networks",
    "⚡ Real-time monitoring with Server-Sent Events (sub-second latency)",
    "🌐 Multi-network support (Helium, Render, Arweave, custom HTTP)",
    "📈 Performance trend analysis and anomaly detection",
    "🔔 Intelligent alerts via Discord, Telegram, and webhooks",
    "📉 Historical analytics with 24h and 7-day insights",
    "🚀 One-command Docker deployment (< 60 seconds)",
    "💯 100% TypeScript with full type safety",
    "🧪 80% test coverage for utilities"
  ],
  "tags": [
    "monitoring",
    "depin",
    "ai",
    "ml",
    "predictive-analytics",
    "earnings-optimization",
    "helium",
    "render",
    "arweave",
    "nextjs",
    "typescript",
    "mongodb"
  ]
}
